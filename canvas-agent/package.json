{
  "name": "@ic/canvas-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "本机 Agent 桥接器：把 Codex app-server / Claude Code CLI 的事件归一化为 IC 的 Item 协议",
  "bin": { "ic-canvas-agent": "./src/cli.js" },
  "scripts": {
    "start": "node src/cli.js",
    "test": "node --test src"
  },
  "engines": { "node": ">=20" }
}
