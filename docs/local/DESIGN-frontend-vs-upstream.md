# ic2 前端 vs 上游 basketikun — 顶层定位

## 关系

| | 仓库 | 本机 | 公网 |
|--|------|------|------|
| 上游参考 | github.com/basketikun/infinite-canvas | `/tmp/basketikun-ic`（只读对照）；生产另有 `/home/box/infinite-canvas-ic` | `ic.zehh.de5.net` |
| Go 重写魔改 | cnb.cool/context-flow.cloud/ic | `/home/box/ic2` | `ic2.zehh.de5.net` |

上游 = **本地优先 SPA**（IndexedDB + 前端持 Key 直连渠道）。  
ic2 = **服务端权威**（Workspace/画布/凭据/执行在 Go），前端应是「好看的客户端」，不是第二套业务真相。

有意偏离见 `docs/upstream/divergences.md`（DIV-01..10）：凭据不上前端、脚本不上浏览器、插件沙箱等 **禁止为了「更像上游」而回退**。

## 问题陈述

契约对等矩阵宣称大量 done，但体感上前端重写「差」——通常不是缺 API，而是：

1. **交互密度**：上游画布页单文件 3k+ 行堆出来的手感（工具条、右键、快捷键、侧栏）重写后拆模块却「空/顿/绕」。
2. **视觉与信息架构**：Ant/布局/空态/加载不如上游打磨。
3. **工作流闭环**：生图/参考图/节点工具链路步骤更多或少反馈。
4. **Agent/工作台**：能力挂了，面板与上游不一致导致「找不到」。

## 策略（选定方向）

**禁止**：整仓把上游 `web/` 贴进 ic2（会带回 DIV-02/03/04 安全回退）。  
**采用**：**体验层对齐上游 + 数据层只走 ic2 API**。

```
上游 UI 模式（对照物）     ic2
  交互/布局/文案/动效   →  复刻或借鉴
  IndexedDB / 前端 Key  →  不复刻（走 /api/v1 + 服务端凭据）
  canvas-proxy CORS     →  不需要（同源）
```

分波：

| 波次 | 主题 | 成功标准 |
|------|------|----------|
| W0 | 基线录屏/清单 | 你指出 Top5「差」的页面或操作；对照上游同路径 |
| W1 | 画布壳 | 侧栏/顶栏/选区工具条/右键菜单密度与手感接近 `ic` |
| W2 | 节点图像工具 | 蒙版/裁剪/反推/多角度等主路径少一步、反馈清晰 |
| W3 | 工作台 | 参考图栏/历史/设置与上游同级可用 |
| W4 | Agent 面板 | 入口、权限模式、工具确认不迷路 |
| W5 | 视觉统一 | 间距、字体、空态、暗色 |

每波：对照上游文件路径 → 在 ic2 对应 `features/*` 改 → 不新增前端持有的明文 Key。

## 对抗

| 风险 | 对策 |
|------|------|
| 为像上游把 Key 放回 localStorage | 硬拦；设置页只掩码 |
| 大段复制上游组件导致双数据源 | 强制 props/API 适配层，禁止直读 IndexedDB |
| 一次改全站失控 | 严格按波次；每波可上线验收 |
| Fable 只能改 GitHub 仓 | 上游可只读对照；改动落在本机 `/home/box/ic2`（或先 mirror 到 GitHub 再用 Fable） |
| 与开放访问混谈 | 开放访问是运维开关；前端体验是另一条线 |

## 与「开放访问 v2」关系

正交。开放访问已可用；体验线不依赖再改登录。可并行，优先体验若你更痛。

## 下一步（需你拍板的最小输入）

只回答一件：当前 **最不能忍的 1～3 个具体操作**（例如「画布右键菜单」「生图参考图排序」「Agent 侧栏」）。  
没有的话默认从 **W1 画布壳** 开干。
